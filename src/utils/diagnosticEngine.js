import {diagnosticRules} from '@site/src/data/diagnostics';
const confidenceWeight={high:8,medium:4,low:1};
const kindWeight={'root-cause':8,'missing-component':4,configuration:3,symptom:0};
const noteMarkers=[/sentinel doctor test notes/i,/likely root causes/i,/expected fix order/i,/recommended repair path/i,/user notes/i,/suggested fix/i];
function resetTest(re,text){re.lastIndex=0;return re.test(text);}
function runtimeLines(text){const lines=text.split(/\r?\n/);let ignore=false;return lines.filter(line=>{if(noteMarkers.some(re=>re.test(line))){ignore=true;return false;}if(ignore&&/^\s*(?:\[?\d{1,2}:\d{2}:\d{2}|fatal|error|warning|info|exception)/i.test(line))ignore=false;return !ignore;});}
function matchingLines(lines,patterns,max=4){return lines.filter(line=>patterns.some(p=>resetTest(p,line))).slice(0,max);}
export function analyzeDiagnostics(input,{context=''}={}){const evidence=runtimeLines(input||'');const evidenceText=evidence.join('\n');const normalized=`${context}\n${evidenceText}`.toLowerCase();let matches=diagnosticRules.map(rule=>{const keywords=(rule.keywords||[]).filter(k=>normalized.includes(k.toLowerCase()));const patterns=(rule.logPatterns||[]).filter(p=>resetTest(p,evidenceText));const required=rule.requirePatterns||[];if(required.length&&!required.some(p=>resetTest(p,evidenceText)))return null;const evidenceLines=matchingLines(evidence,[...(rule.logPatterns||[]),...required]);if(!keywords.length&&!patterns.length&&!evidenceLines.length)return null;let score=keywords.length*2+patterns.length*6+(confidenceWeight[rule.confidence]||0)+(kindWeight[rule.kind]||0)+(rule.specificity||0);if(rule.id==='ragenativeui'){if(/ragenativeui[\s\S]{0,300}installed\s*:\s*[\d.]+[\s\S]{0,120}required\s*:\s*[\d.]+\+?/i.test(evidenceText))score+=20;if(/missingmethodexception/i.test(evidenceText)&&/ragenativeui\./i.test(evidenceText))score+=18;if(/failed to initialize/i.test(evidenceText)&&/ragenativeui/i.test(evidenceText))score+=8;}if(rule.id==='heap-adjuster-missing'&&/heap\s*adjuster.*(?:not detected|missing|not found)/i.test(evidenceText))score+=8;if(rule.id==='gameconfig-packfiles'&&/hash does not match|incorrect gameconfig/i.test(evidenceText))score+=8;return {...rule,score,evidenceLines};}).filter(Boolean);const ids=new Set(matches.map(m=>m.id));const suppressed=new Set();matches.forEach(m=>(m.suppresses||[]).forEach(id=>ids.has(id)&&suppressed.add(id)));if(ids.has('ragenativeui')){suppressed.add('lml-eup');suppressed.add('missing-dependency');}if(/loaded:\s*[^\r\n]+\.dll/i.test(evidenceText)&&!/plugin.*(?:not found|failed to load plugin|could not load plugin)/i.test(evidenceText))suppressed.add('wrong-plugin-folder');matches=matches.filter(m=>!suppressed.has(m.id));const hasSpecific=matches.some(m=>(m.specificity||0)>=3&&!['generic-crash','performance'].includes(m.id));if(hasSpecific)matches=matches.filter(m=>!['generic-crash','performance'].includes(m.id));matches.sort((a,b)=>b.score-a.score);return {matches,ignoredLineCount:(input||'').split(/\r?\n/).length-evidence.length};}
