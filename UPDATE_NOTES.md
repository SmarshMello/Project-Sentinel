# Sentinel AI enhanced update

Changes:

- Installed mod/plugin selector now searches the entire Project Sentinel database.
- All database records can be selected, not only Sentinel Police entries.
- Select-visible and clear-all controls added.
- Build Planner selections are saved automatically in the browser.
- Sentinel AI automatically loads the latest Build Planner selection.
- Manual reload button and selected-count display added.
- Complete Worker/OpenAI/Turnstile setup guide added.
- Safe local-development secret example added.

Validated with `npm ci --no-audit --no-fund` and `npm run build`.
