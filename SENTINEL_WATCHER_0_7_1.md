# Sentinel Watcher 0.7.1

## Dashboard publishing fix

- The dashboard now keeps polling GitHub Pages after a scan completes instead of refreshing only once after 12 seconds.
- Intelligence cards populate as soon as the new schema 6 report is actually live.
- A deployment status message explains whether the report is publishing, live, or still delayed.
- Intelligence and category totals include safe client-side fallbacks when fields are absent.

No files need to be deleted.
