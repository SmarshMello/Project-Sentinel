---
title: Required Plugin Attribute Error
---

`StopThePed.dll does not have the required Plugin attribute` means RPH attempted to load an LSPDFR plugin directly. Place it under `plugins/LSPDFR`, disable load-all behavior and let LSPDFR load it when going on duty.
